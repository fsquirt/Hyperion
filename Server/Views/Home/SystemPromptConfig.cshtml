<div class="p-3">
    <div class="card shadow-sm">
        <div class="card-header bg-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><i class="bi bi-chat-left-text me-2"></i>大模型系统提示词配置</h5>
            <div>
                <span id="sp-updated" class="text-muted small me-2"></span>
                <button id="sp-save" class="btn btn-primary btn-sm">
                    <i class="bi bi-save me-1"></i>保存
                </button>
            </div>
        </div>
        <div class="card-body">
            <div class="alert alert-info small mb-3">
                该提示词由 <code>MSAFReverseAgent</code> 在每次分析时从服务端拉取。Agent 会按样本类型自动选择对应提示词：
                <b>.exe / .dll / .ocx / .pyd</b> 使用「用户态样本提示词」，
                <b>.sys</b> 使用「内核态样本提示词」。修改后保存,Agent 下一次分析即生效。
            </div>

            <div class="mb-4">
                <label class="form-label fw-bold">用户态样本提示词（.exe / .dll / .ocx / .pyd）</label>
                <textarea id="sp-exe" class="form-control font-monospace" style="min-height:320px; font-size:13px;" placeholder="加载中..."></textarea>
                <div class="form-text">
                    重点:该样本因<strong>触发驱动通信</strong>被捕获,应引导 Agent 重点还原驱动通信方式
                    (DeviceIoControl / IOCTL 控制码 / 设备路径 \\\\.\GlobalRoot 等)。
                </div>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">内核态样本提示词（.sys）</label>
                <textarea id="sp-sys" class="form-control font-monospace" style="min-height:320px; font-size:13px;" placeholder="加载中..."></textarea>
                <div class="form-text">
                    重点:引导 Agent 重点核查驱动是否提供<strong>任意内存读写原语</strong>
                    (MmMapIoSpace / MmCopyMemory / ZwWriteVirtualMemory / MmCopyVirtualMemory 等)。
                </div>
            </div>

            <div id="sp-status" class="small"></div>
        </div>
    </div>
</div>

<script src="/js/system-prompt-config.js" asp-append-version="true"></script>
