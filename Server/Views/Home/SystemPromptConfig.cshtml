<div class="p-3">
    <div class="card shadow-sm">
        <div class="card-header bg-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><i class="bi bi-chat-left-text me-2"></i>大模型系统提示词配置</h5>
            <div>
                <span id="sp-updated" class="text-muted small me-2"></span>
                <button id="sp-save" class="btn btn-primary btn-sm">
                    <i class="bi bi-save me-1"></i>保存
                </button>
            </div>
        </div>
        <div class="card-body">
            <div class="alert alert-info small mb-3">
                该提示词由逆向分析 Agent 在每次分析时从服务端拉取，用于指导主 Agent 的分析流程。
                主 Agent 会根据上下文（IOCTL 记录、设备列表、进程树快照）启动子 Agent 分析取证文件，
                最终出具会话级总结报告。修改后保存，Agent 下一次分析即生效。
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">反作弊逆向分析系统提示词</label>
                <textarea id="sp-exe" class="form-control font-monospace" style="min-height:480px; font-size:13px;" placeholder="加载中..."></textarea>
                <div class="form-text">
                    统一提示词，不再区分用户态/内核态。重点引导 Agent 往反作弊方向分析：
                    动态函数解析、内存读写原语、服务创建、BYOVD 利用、.data 段附加文件、证书检查、IOCTL 后门。
                    动态 Minidump 作为辅助分析手段。
                </div>
            </div>

            <div id="sp-status" class="small"></div>
        </div>
    </div>
</div>

<script src="/js/system-prompt-config.js" asp-append-version="true"></script>
