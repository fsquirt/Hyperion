# Hyperion 逆向分析 Agent 依赖
# 安装：pip install -r requirements.txt

# Agent 编排框架（openai-agents-python）
openai-agents>=0.19.0
openai>=1.99.0

# MCP 协议客户端（openai-agents 依赖，显式锁定）
mcp>=1.9.0

# WinDbg / CDB 崩溃转储分析 MCP 服务器，安装后提供 `mcp-windbg` 命令
mcp-windbg>=1.0.0

httpx>=0.27.0
pydantic>=2.7.0

# IDA Pro MCP 需在 IDA 侧安装插件：
#   pip install git+https://github.com/mrexodia/ida-pro-mcp.git
#   ida-pro-mcp --install
